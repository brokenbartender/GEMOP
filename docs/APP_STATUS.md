# App Status

This file is generated by `scripts/update_app_status.py`.
It is the single source of truth for the current runtime shape and counts.

## Entry Points
- `start.ps1 -Council` (multi-agent council orchestrator)

## Intelligence Routing
- Council hybrid router: `scripts/agent_runner_v2.py`
- Local fallback model (default): `phi4` via Ollama

## Governance
- Council supervisor (deterministic verify/challenge + safe summaries): `scripts/council_supervisor.py`
- Patch auto-apply (fail-closed): `scripts/council_patch_apply.py`

## Reliability / Red-Team
- Council bus (jsonl message bus): `scripts/council_bus.py`
- World-state rebuilder (blackout/drift resilience): `scripts/state_rebuilder.py`

## Skills
- Local skills (`agents/skills/skill_*.py`): **0**
- Global skills (skills registry): **1267**
- External skills sources (bridged into council prompts): `C:\Users\<you>\.codex\skills`, `C:\Users\<you>\.gemini\skills` (override via `GEMINI_OP_SKILLS_DIR_CODEX` / `GEMINI_OP_SKILLS_DIR_GEMINI`).
- Skill selection + pack rendering: `scripts/skill_bridge.py` -> `<run_dir>/state/skills_selected.md` (auto-injected by `scripts/triad_orchestrator.ps1`).

## UIs
- Streamlit dashboard: `scripts/dashboard.py` (includes `GOVERNANCE`, `SKILLS`, and `DOCS` tabs)
- TUI dashboard: `scripts/tui_dashboard.py` (includes a `DOCS` panel rendering `docs/APP_STATUS.md`)

## Config
- Shared profiles: `configs/config.base.toml`, `configs/config.core.toml`, `configs/config.full.toml`, `configs/config.max.toml`
- Local overrides (ignored): `configs/config.local.toml`
- Runtime assembled (ignored): `configs/config.active.toml`

## Doc Policy
- Any functional change must be accompanied by doc updates.
- The pre-commit hook auto-refreshes this file and will block commits if it is stale.
- Launch-time guardrails: `scripts/check_docs.ps1` is executed by `start.ps1` and `scripts/sovereign_brain.py` (unless skipped).
