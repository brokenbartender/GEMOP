# Market Strategy Context

This directory stores generated market context artifacts for trading decisions.

- `market_context.json` is produced by `skill_market_strategist.py`.
- Files are local runtime data and are intentionally gitignored.
