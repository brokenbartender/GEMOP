# Local State

This directory stores runtime memory/state generated by the agent pipeline.

Examples:
- `product_history.json` (librarian dedup memory)
- `strategy.json` (strategist feedback rules)

These `.json` files are intentionally gitignored so local learning signals do not pollute commits.
